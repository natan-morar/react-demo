import React, { useState } from 'react';

function DynamicInput(props: { element: Frontier.Element }) {
    return (
        <div>
            <input></input>
        </div>
    );
}

export default DynamicInput;
